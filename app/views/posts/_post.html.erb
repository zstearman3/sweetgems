<div class="post">
  <h2><%= post.title %></h2>
  <small><%= post.created_at.to_date.to_formatted_s(:long_ordinal) %></small>
  <div class="post-body"><%= truncate(post.content.html_safe, :length => 1000, :escape => false) %></div>
  <%= link_to "Full Post", post %>
</div>